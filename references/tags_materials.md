# Material Types — mph Feature Tags

Extracted from mph tags.json (COMSOL 6.4).
Feature type strings for use with `create(tag, type_string, dim)`.

Tag: `material`

### LayeredMaterialStack
Tag: `stlmat*`
  Tag: `stlmat*`
  - `LayeredMaterial` -> `lmat*`
  - `LayeredMaterialStackLink` -> `stllmat*`

### PorousMedia
Tag: `poromat*`
  Tag: `poromat*`
  - `Fluid` -> `fluid*`
  - `ImFluid` -> `imfluid*`
  - `Solid` -> `solid*`

### Switch
Tag: `sw*`
  Tag: `sw*`
  - `Common` -> `mat*`
  - `LayeredMaterial` -> `lmat*`
- `Common` -> `mat*`
- `LayeredMaterial` -> `lmat*`
- `LayeredMaterialLink` -> `llmat*`
- `Link` -> `matlnk*`
